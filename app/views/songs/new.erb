<h4>
  <form action=/'songs' method=post>
    <label>Song</label>
    <input type="text" name="Name" placeholder="Song Name:"><br><br>
    <label>Artist</label>
    <input type="text" name="Artist Name" placeholder="Artist Name:"><br><br>
    <label>Genres</label>
    <% Genre.all.each do |genre| %>
      <input type="checkbox" id="<%= genre.name %>" name="genres[]" value="<%= genre.id %>">
    <% end %><br><br>

    <input type="submit" value="Create Song">
  </form>
</h4>
